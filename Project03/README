解压文件到本地后运行以下指令即可:
gcc -o mips_sim mips_sim.c
./mips_sim


主存地址范围对应数据段
data
0x00000-0x01000
text
0x01000-0x03000
heap
...
stack
0x06000-0x80000

$gp指针被用作指向data段初始为0
模拟器会从data文件中读取并将数据存到主存的开始

实现了父子进程通信
子进程实现了时序模拟并且能够正确打印流水线运行状态

./mips_sim > res.txt
在res.txt中可以看到指令的执行情况，包括流进流水线的指令OPCODE,周期数,每个流水段的指令类型
在模拟完父进程提供的指令序列后子进程将会不断将NOP指令送入流水线，直到流水线空闲，计算出正确的周期数


在子进程的时序模拟中完成数据cache的模拟